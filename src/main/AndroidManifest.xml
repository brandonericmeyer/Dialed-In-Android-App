<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.regis.helloworld.dialedin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".GenerateActivity"
            android:label="@string/generate_workout_title"
            android:parentActivityName=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar.MainStyle"></activity>
        <activity android:name=".ShareActivity"
            android:label="@string/share_workout_title"
            android:parentActivityName=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar.MainStyle"></activity>
        <activity android:name=".ModifyActivity"
            android:label="@string/modify_workout_title"
            android:parentActivityName=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar.MainStyle"></activity>
        <activity
            android:name=".RemoveActivity"
            android:label="@string/remove_workout_title"
            android:parentActivityName=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar.MainStyle"/>
        <activity
            android:name=".AddActivity"
            android:label="@string/add_workout_title"
            android:parentActivityName=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar.MainStyle"/>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar.MainStyle">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>